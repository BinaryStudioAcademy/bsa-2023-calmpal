//NOTE: This type is required for "react-native-track-player" library, so don't change it
type Track = {
  id: string;
  url: string;
  title: string;
  artist: string;
  artwork: string;
};

export { type Track };
