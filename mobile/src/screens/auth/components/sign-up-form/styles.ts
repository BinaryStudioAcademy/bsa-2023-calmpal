import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  formContainer: {
    height: '100%',
    width: '100%',
  },
  titleText: {
    fontSize: 35,
    color: 'white',
    marginBottom: 30,
    marginTop: 30,
  },
});

export { styles };
