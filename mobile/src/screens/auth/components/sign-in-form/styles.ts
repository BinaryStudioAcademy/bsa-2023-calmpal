import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  formContainer: {
    height: '100%',
    width: '100%',
    paddingHorizontal: 44,
  },
  titleText: {
    fontSize: 30,
    color: 'white',
    marginBottom: 30,
    marginTop: 30,
  },
  bottomText: {
    fontSize: 14,
  },
  boldText: {
    fontWeight: 'bold',
  },
});

export { styles };
