import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  formContainer: {
    height: '100%',
    width: '100%',
    paddingHorizontal: 55,
  },
  titleText: {
    fontSize: 30,
    color: 'white',
    marginBottom: 30,
    marginTop: 30,
  },
  boldText: {
    fontWeight: 'bold',
  },
});

export { styles };
