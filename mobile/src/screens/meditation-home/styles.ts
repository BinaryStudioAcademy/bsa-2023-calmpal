import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingTop: 12,
    paddingHorizontal: 12,
    flex: 1,
  },
  contentContainer: {
    marginBottom: 60,
  },
  list: {
    paddingHorizontal: 4,
  },
  linearGradient: {
    flex: 1,
  },
});

export { styles };
