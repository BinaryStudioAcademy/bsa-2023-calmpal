import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingBottom: 16,
    paddingTop: 28,
  },
  list: {
    paddingHorizontal: 16,
  },
});

export { styles };
