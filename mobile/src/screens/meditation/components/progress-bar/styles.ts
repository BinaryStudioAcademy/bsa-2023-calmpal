import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    position: 'relative',
    width: '100%',
    marginTop: 40,
    marginBottom: 20,
  },
  progress: {
    position: 'absolute',
    left: 17,
    bottom: 20,
  },
  duration: {
    position: 'absolute',
    right: 17,
    bottom: 20,
  },
});

export { styles };
