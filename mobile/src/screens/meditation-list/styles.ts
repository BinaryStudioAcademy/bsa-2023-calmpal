import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingTop: 28,
    flex: 1,
    paddingBottom: 16,
  },
  list: {
    paddingHorizontal: 8,
  },
});

export { styles };
